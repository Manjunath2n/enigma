// Optional Firebase config. If left unconfigured, the app falls back to localStorage-based transport.
// To enable Firebase Realtime Database, fill in your project credentials below and ensure a database
// exists with permissive rules for testing (lock down before production!).

/* Example:
const firebaseConfig = {
  apiKey: "...",
  authDomain: "...",
  databaseURL: "https://<your-db>.firebasedatabase.app",
  projectId: "...",
  storageBucket: "...",
  messagingSenderId: "...",
  appId: "..."
};
firebase.initializeApp(firebaseConfig);
window.db = firebase.database();
*/


